import { StylizeNumberDirective } from './stylize-number.directive';

describe('StylizeNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new StylizeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
